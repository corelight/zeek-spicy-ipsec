# This file sets up protocol confirmation/rejection for analyzers.

module Zeek_zeek_spicy_ipsec;

import zeek_spicy_ipsec;
import zeek;

# TODO: For DPD, protocol analyzers should confirm or reject a protocol.
# on zeek_spicy_ipsec::IPSEC::%done {
#     zeek::confirm_protocol();
# }
#
# on zeek_spicy_ipsec::IPSEC::%error {
#     zeek::reject_protocol("error while parsing IPSEC record");
# }
